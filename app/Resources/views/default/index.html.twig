{% extends 'base.html.twig' %}

{% block header %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="{{ path('homepage') }}"><i class="fas fa-plane"></i> Flyaround</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('planemodel_index') }}">Planemodels<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('reservation_index') }}">Reservations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('flight_index') }}">Flights</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('site_index') }}">Sites</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('review_index') }}">Reviews</a>
                </li>
            </ul>
        </div>
    </nav>
{% endblock %}

{% block stylesheets %}
{% endblock %}
